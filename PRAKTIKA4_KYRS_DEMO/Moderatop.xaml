<Window x:Class="PRAKTIKA4_KYRS_DEMO.Moderatop"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PRAKTIKA4_KYRS_DEMO"
        mc:Ignorable="d"
            Title="Мероприятия" MinHeight="450" MinWidth="800"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Label Grid.Row="0" Content="Мероприятия" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center"/>
        <ScrollViewer Grid.Row="1">
            <ItemsControl x:Name="EventsList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel  Margin="5">
                            <Image Source="{Binding LogoPath}" Width="70" Height="70" Margin="0,0,10,0"/>
                            <StackPanel>
                                <TextBlock Text="{Binding NameEvent}" FontSize="16" FontWeight="Bold"/>
                                <TextBlock Text="{Binding DirectionName}" Foreground="Gray"/>
                                <TextBlock Text="{Binding Date}" Foreground="Gray"/>
                            </StackPanel>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <Button Grid.Row="2" Content="Назад" Click="Button_Click" Margin="10"/>
    </Grid>
</Window>
